https://leetcode.com/problems/the-number-of-rich-customers/

# Write your MySQL query statement below
/**

ONE TABLE: Store
bill_id is the primary key for Store table.
Store table contains information about the amount of one bill and the customer associated with it.

STEPS:
    COUNT(DISTINCT)
    WHERE

*/

SELECT 
    COUNT(DISTINCT customer_id) AS rich_count
FROM Store
WHERE amount > 500
